#
# Git-managed by hosts/infra/cockpit/install.sh.
# Advertise only on CT110's primary LAN address, not the Pi-hole service address
# or its Docker bridges.
#
WSDD_PARAMS="-i 192.168.0.110 -4 -w WORKGROUP"
